// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  // BASE_API_ENDPOINT: 'http://dummy.restapiexample.com/api/v1/',
  // BASE_API_ENDPOINT: 'http://104.211.219.149:1432/api/',
  BASE_API_ENDPOINT: 'https://eliteerp-api.dedicateddevelopers.us/frontend/',
  BASE_IMAGE_ENDPOINT: 'https://ellite.s3-us-west-1.amazonaws.com/',
  production: false,
  DOMAIN: 'localhost',
  countryCode: '+91'
};
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
















